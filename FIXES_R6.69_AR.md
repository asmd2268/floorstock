# إصلاحات ASDHealth FloorStock — R6.69.0

## Print Orders

- نقل محرك إنشاء PDF من JavaScript داخلي داخل نافذة `about:blank` إلى صفحة ثابتة وملف JavaScript خارجيين متوافقين مع سياسة CSP.
- تمرير بيانات الطلبات إلى صفحة الطباعة بمفتاح مؤقت في التخزين المحلي، مع مسار احتياطي آمن من نافذة التطبيق، وحذف البيانات المؤقتة بعد قراءتها.
- إنهاء حالة التعليق عند `Preparing the final A4 landscape PDF…`؛ أي خطأ أو تجاوز للمهلة يظهر الآن رسالة واضحة بدل بقاء الصفحة معلقة.
- جعل أبعاد صفحة PDF تطابق الاتجاه الذي اختاره محرك التخطيط فعلًا: A4 Landscape أو A4 Portrait.
- الإبقاء على السلوك المطلوب دون تغيير: عمود لكل طلب، صفحة واحدة، السماح بالتصغير القسري، ومنطق اختيار عدد الأعمدة الحالي.
- إضافة اختبار انحداري يمنع إعادة استخدام inline script أو `document.write` لمسار Print Orders ويتحقق من اتجاه PDF ومعالجة الأخطاء.
- رفع كسر التخزين المؤقت إلى `R6.69.0`.
