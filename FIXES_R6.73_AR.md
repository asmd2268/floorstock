# إصلاحات ASDHealth FloorStock R6.73.0

## نطاق الإصلاح

- إصلاح رفض `crash_carts` في بوابة الواجهة لمشرف صيدلية التنويم والماستر.
- إصلاح عملية Merge لأسماء الأدوية في Inventory لمشرف صيدلية التنويم.
- إظهار وتشغيل طباعة Shelves لحسابات الأقسام ضمن قسمهم فقط.

## السبب الجذري

بوابة الحفظ في الواجهة كانت تبني قرار الصلاحية من حالة مستخدم قديمة أو مؤقتة، كما كانت تشترط وجود `FB_AUTH.currentUser` داخل البوابة المحلية. هذا قد يرفض العملية قبل إرسالها إلى Firestore رغم أن قواعد Firestore تسمح بها. كما لم تكن أسماء دور `master` وبعض الأسماء البديلة موحدة في جميع المسارات.

## التغييرات

1. إنشاء ملف صلاحية فعلي موحد عبر `resolvePermissionProfile`.
2. اعتماد المستخدم الفعلي للماستر، والمستخدم الفعّال فقط أثناء Master Test Mode.
3. إزالة الاعتماد المحلي على `FB_AUTH.currentUser` من قرار واجهة الحفظ؛ الحماية الفعلية تبقى في Firebase Authentication وقواعد Firestore.
4. توحيد أسماء الأدوار البديلة، ومنها `master` و`inpatient pharmacy supervisor`.
5. إضافة حواجز صريحة قبل فتح وتنفيذ Merge.
6. إضافة زر طباعة ظاهر أعلى صفحة Shelves وبطاقة طباعة ثابتة داخل الصفحة.
7. حصر طباعة Shelves في حساب القسم وبيانات قسمه فقط.
8. تشغيل نافذة الطباعة من الصفحة الأصلية بدل سكربت inline داخل نافذة الطباعة، لتفادي CSP.

## التحقق

- اختبارات واجهة وانحدار: 25/25 ناجحة.
- فحص صياغة JavaScript: 81 ملفًا دون أخطاء.
- فحص مسارات استيراد ES Modules المحلية: دون مسارات مفقودة.
- قواعد Firestore لم تتغير عن R6.72.1، والتي اجتازت على جهاز المستخدم 80/80 اختبارًا.

## النشر

هذا الإصدار تعديل واجهة فقط. يكفي نشر Vercel. لا يلزم نشر Firestore Rules أو Functions مرة أخرى إذا كانت R6.72.1 منشورة بالفعل.
