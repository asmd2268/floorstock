# إصلاحات ASDHealth FloorStock — R6.66.0

## المشاكل التي عولجت

- إصلاح فشل النسخة الاحتياطية الذي كان يمنع الحذف النهائي لعربة الطوارئ، خصوصًا في Chrome عند إغلاق معاملة IndexedDB قبل اكتمال التنظيف.
- إبقاء النسخة المتولدة قابلة للتنزيل حتى لو تعذر تخزينها محليًا في IndexedDB أو تعذر حفظ مؤشرها في `localStorage`.
- إصلاح بوابة حفظ المخزون وربط `setMeds` بالبوابة الرسمية `inventorySafetySetMeds` بدل بقاء الدالة المحلية على حالة «غير مهيأة».
- تصحيح صلاحيات `inpatient_supervisor` لبيانات حماية المخزون والتصنيفات وشعار المنشأة، ومنع تحميل قائمة المستخدمين للحسابات التي لا تملك صلاحيتها.
- جعل فتح صفحة Schedule عملية قراءة فقط، وعدم تنفيذ ترحيل أو حذف مفاتيح قديمة تلقائيًا عند فتح الصفحة.
- خصم الكميات المبلغ عنها في Crash Cart فور إرسال البلاغ، مع تسجيل تفاصيل الدفعات، ثم جعل إغلاق البلاغ يضيف التعويض فقط دون خصم مكرر.
- إرجاع كميات البلاغ تلقائيًا إذا فشل حفظ البلاغ بعد تحديث العربة.
- تحسين مطابقة قسم NICU مع التسميات القديمة والبديلة العربية والإنجليزية لكي تظهر عرباته للحساب المرتبط بالقسم.
- حماية بيانات New Request وBulk opening & replacement من التحديث التلقائي، وحفظ مسودة مؤقتة مرتبطة بالحساب داخل الجلسة.
- تحسين مسار الحفظ في Chrome باستخدام الاكتشاف التلقائي للـ long polling وفصل مسار الكتابة عن مسار القراءة الاحتياطي.
- تحسين تسجيل الخروج: انتظار محدود للحفظ، منع الضغط المتكرر، وعدم مسح الواجهة قبل نجاح تسجيل الخروج.
- رفع رقم كسر التخزين المؤقت إلى `R6.66.0` حتى تُحمّل المتصفحات الملفات المعدلة بعد النشر.

## التحقق المنفذ

- نجحت 15 من 15 اختبارات واجهة وانحدار.
- نجح فحص بنية المستودع والصلاحيات وعلامات الأمان.
- نجح فحص 66 ملف ES Module ومسارات الاستيراد والربط.
- نجح فحص الصياغة لجميع ملفات JavaScript في `public/assets/js`.

## قبل اعتماد النسخة في الإنتاج

يجب نشر Hosting وقواعد Firestore معًا، لأن جزءًا من إصلاح صلاحيات المشرف موجود في `firestore.rules` وليس في الواجهة فقط.

تعذر تشغيل اختبارات Firestore Emulator وPlaywright داخل بيئة الإصلاح بسبب عدم توفر حزم npm من السجل الداخلي ومنع المتصفح المحلي من فتح خادم localhost. لذلك يلزم تشغيل `npm test` في بيئة التطوير المعتادة قبل النشر النهائي.
