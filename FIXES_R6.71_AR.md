# إصلاحات ASDHealth FloorStock — R6.71.0

## صلاحيات مشرف صيدلية التنويم

وُحّدت صلاحيات الدور `inpatient_supervisor` وأسماء الدور البديلة داخل طبقة قدرات واحدة، وربطت بها الواجهة وبوابة الكتابة وقواعد Firestore.

المسموح للمشرف:

- قراءة وإدارة Inventory لجميع الأقسام، بما يشمل الأدوية والكميات والأرفف والصلاحيات والتنبيهات والتصنيفات والإخفاء والتجميد ودمج الأسماء.
- إدارة الطلبات وتنفيذها وطباعتها وتحليلها.
- تشغيل وإعداد Crash Cart ومعالجة البلاغات والفتح والاستبدال الجماعي.
- إدارة Medication Accountability وإنشاء رموز الاستلام والتسليم المؤقتة.
- قراءة Schedule دون تعديل أوقات الطلب أو حدوده.

غير المسموح للمشرف:

- إدارة المستخدمين والأقسام.
- إدارة الأدوية الخاضعة للرقابة.
- تعديل Schedule.
- النسخ الاحتياطي والاستعادة وحذف Crash Cart نهائيًا.

أصبح تنبيه رفض الصلاحية يذكر مفتاح البيانات الذي رُفض، بدل رسالة عامة فقط، لتسهيل التشخيص.

## Medication Accountability المستقل

يمكن كتابة اسم علاج جديد داخل Accountability حتى لو لم يكن موجودًا في Inventory. يبقى العلاج داخل مفاتيح Accountability فقط ولا يُنشأ داخل `meds_<department>`.

## تأكيد الاستلام والتسليم برمزي QR مؤقتين

- تنشئ الصيدلية جلستين منطقيتين داخل عملية واحدة: رمز لتسليم الصيدلية ورمز لاستلام القسم.
- الرابطان لا يحتاجان تسجيل دخول أو OTP، بناءً على الطلب.
- كل رابط يستخدم Token عشوائيًا مختلفًا، ويُحفظ في Firestore على هيئة SHA-256 فقط.
- مدة الصلاحية الافتراضية 30 دقيقة، ويمكن إعادة إصدار الرمزين بعد الانتهاء.
- يكتب كل طرف الاسم والرقم الوظيفي ويؤكد مرة واحدة.
- لا يُعوض رصيد العهدة ولا يُقفل سجل الاستخدام إلا بعد تأكيد الطرفين.
- لا يستطيع العميل قراءة أو تعديل جلسات التسليم مباشرة في Firestore؛ جميع العمليات تمر عبر Cloud Functions.
- يحتوي الرابط الحساس على Fragment بعد `#` حتى لا يصل Token إلى Vercel عند فتح الصفحة.

## الاختبارات المنفذة

- 21/21 اختبار واجهة وسياسات صلاحيات.
- 4/4 اختبارات منطق Cloud Functions للتوكن، التأكيد الجزئي، منع التكرار، والتعويض بعد الطرفين.
- فحص 72 وحدة ES Module ومسارات الاستيراد.
- فحص بنية المشروع وصياغة JavaScript.
- اختبار Chromium فعلي لصفحة QR العامة: تحميل التفاصيل، إزالة الرمز من شريط العنوان، وإرسال تأكيد طرف واحد دون تسجيل دخول.

تعذر تشغيل Firestore Emulator الكامل داخل بيئة التجهيز لأن تنزيل اعتماديات npm كان محجوبًا من سجل البيئة، لذلك يجب تشغيل `npm run test:rules` محليًا قبل النشر. توجد اختبارات قواعد مضافة تغطي منع الوصول المباشر لجلسات QR ومصفوفة صلاحيات الأدوار.

## النشر المطلوب

هذه النسخة تضيف Cloud Functions وقواعد Firestore، لذلك لا يكفي نشر Vercel وحده:

```bash
npx firebase-tools deploy --project floorstock-6ac2d --only firestore:rules,functions
npx vercel@latest --prod
```
